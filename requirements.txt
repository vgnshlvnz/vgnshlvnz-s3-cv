# Lambda Function Dependencies for Job Tracker API
# Security: JWT token validation and cryptographic operations

# JWT validation with Cognito
PyJWT==2.10.1
cryptography==44.0.0

# AWS SDK
# Note: Lambda runtime includes boto3/botocore. Only specify if you need a specific version.
# Using latest versions for security patches.
boto3==1.35.94
botocore==1.35.94
